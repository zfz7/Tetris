#Used for deploy on an E2C instance via Docker-compose
spring:
  datasource:
    url: jdbc:postgresql://postgres_db:5432/tetris
    username: tetris
    password: tetris
  jpa:
    hibernate:
      ddl-auto: validate
server:
  ssl:
    key-store: /etc/letsencrypt/live/www.example.com/keystore.p12
    key-store-password: tetris
    keyStoreType: PKCS12
    keyAlias: tomcat